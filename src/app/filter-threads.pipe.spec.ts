import { FilterThreadsPipe } from './filter-threads.pipe';

describe('FilterThreadsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterThreadsPipe();
    expect(pipe).toBeTruthy();
  });
});
